fn main(){
    // exercises about bool and char
    
    // exercise 1
    // complete the code below (replace __ to correct answer)
    // fix the code below to make it work
    let f:__ = true;
    let f2:bool = __; // put a valid value here
    let my_char:char = __; // put a valid value here

    // exercise 2
    // how many bytes are necessary to store a bool value?

    // exercise 3
    // how many bytes are necessary to store a char value?

    // exercise 4
    // whats is the value you need put in myEquivalentValue to print the same value of c1?
    // complete the code below (replace __ to correct answer)
    let c1 = 'F';
    let myEquivalentValue:u32 = __;
    println!("Hex({0:032b}) = Hex({1:032b})", c1 as u32, myEquivalentValue);
}